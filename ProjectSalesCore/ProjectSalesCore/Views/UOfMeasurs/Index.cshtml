@model IEnumerable<CSales.Database.Models.UOfMeasur>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnitName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UnitName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUnitOfMeasurement }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUnitOfMeasurement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUnitOfMeasurement })
        </td>
    </tr>
}

</table>

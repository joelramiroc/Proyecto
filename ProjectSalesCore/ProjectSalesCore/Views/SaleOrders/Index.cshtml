@model IEnumerable<CSales.Database.Models.SaleOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "SelectStorage")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentCondition.ConditionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentCondition.ConditionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSaleOrder }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSaleOrder }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdSaleOrder })
            @Html.ActionLink("Generate Sale", "SaleByDispatchs", new { id = item.IdSaleOrder })
        </td>
    </tr>
}

</table>
